import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // This component doesn't have visual styles as it's a utility component
  // All styling is handled by the native image picker and crop picker
});

export default styles;
